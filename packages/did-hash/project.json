{
  "name": "did-hash",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/did-hash/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/rollup:rollup",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/did-hash",
        "main": "packages/did-hash/src/index.ts",
        "tsConfig": "packages/did-hash/tsconfig.lib.json",
        "assets": [],
        "project": "packages/did-hash/package.json",
        "compiler": "swc",
        "format": ["cjs", "esm"],
        "buildableProjectDepsInPackageJsonType": "dependencies"
      }
    },
    "pre-publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/pre-publish.mjs did-hash {args.ver} {args.tag}"
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs did-hash {args.ver} {args.tag}"
      },
      "dependsOn": [
        {
          "projects": "self",
          "params": "forward",
          "target": "pre-publish"
        },
        {
          "projects": "self",
          "params": "ignore",
          "target": "build"
        }
      ]
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/did-hash/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/did-hash/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
